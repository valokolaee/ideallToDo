import { BaseBackGroundColorType } from './types/BaseBackGroundColorType';
import { CurrentLocation } from './types/CurrentLocation';
import { RoutType } from './types/routType';

export type GlobalContentType = {

  baseBackGroundColor: BaseBackGroundColorType;
  currentLocation: CurrentLocation;

  Route: RoutType;

};
